
// ge the page to respond to the user log in status 
/** that if the usser is loged in to display a logout option
 * else if not registered a sign in
 * else if not ogged in a log in option
 */

/** SOLUTION
 * 1.create a database 
 * 2.create two apis one for GET and for post
 * 3.use props to determine the state of the usser
 * 4.return the apropriate prompt
 * 
 */
function Users(){
    

    return(
        <div></div>
        
    );
}
export defaul Users